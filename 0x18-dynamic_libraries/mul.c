#include <stdio.h>

/**
 * mul = the multiplication of the two intergers
 * @a bieng the first integer
 * @b is the second interger
 * returns the multiplied integers
 */

int mul(int a, int b)
{
  return (a * b);
}
